{
  "name": "lisab.dev",
  "version": "0.0.1",
  "scripts": {
    "install": "bundle install",
    "build": "bliss b",
    "start": "bliss s",
    "clean": "rimraf _build && rimraf _site"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lis-b/lisab.dev.git"
  },
  "dependencies": {
    "http-server": "^14.0.0",
    "jekyll-bliss": "^2.0.0",
    "rimraf": "^3.0.2"
  }
}
